\begin{filecontents*}[force]{manuscript.bib}
@misc{fema_nfip_claims_v2,
  author       = {{Federal Emergency Management Agency}},
  title        = {{OpenFEMA Dataset: FIMA NFIP Redacted Claims - v2}},
  year         = {2026},
  howpublished = {\url{https://www.fema.gov/openfema-data-page/fima-nfip-redacted-claims-v2}},
}
@misc{fred_dpcerd3q086sbea,
  author       = {{U.S. Bureau of Economic Analysis}},
  title        = {Personal Consumption Expenditures: Implicit Price Deflator [DPCERD3Q086SBEA]},
  howpublished = {Retrieved from FRED, Federal Reserve Bank of St. Louis},
  year         = {2026},
  url          = {https://stlouisfed.org},
}

@article{fall_analysis_2023,
author = {Fall, Greg and Kitzmiller, David and Pavlovic, Sandra and Zhang, Ziya and Patrick, Nathan and St. Laurent, Michael and Trypaluk, Carl and Wu, Wanru and Miller, Dennis},
title = {The Office of Water Prediction's Analysis of Record for Calibration, version 1.1: Dataset description and precipitation evaluation},
journal = {JAWRA Journal of the American Water Resources Association},
volume = {59},
number = {6},
pages = {1246-1272},
keywords = {hydrology, meteorology, modeling, calibration, model forcing, precipitation, observations, geospatial analysis, geostatistics, time-series analysis, quality assurance/quality control (QAQC)},
doi = {https://doi.org/10.1111/1752-1688.13143},
url = {https://onlinelibrary.wiley.com/doi/abs/10.1111/1752-1688.13143},
eprint = {https://onlinelibrary.wiley.com/doi/pdf/10.1111/1752-1688.13143},
abstract = {Abstract Hydrologic models operated by the National Weather Service call for an accurate, consistent, high-resolution, multi-decade, continental-scale record of hydrometeorological fields to serve as forcing data for model calibration. To serve this purpose, the Analysis of Record for Calibration was developed, and version 1.1 of the dataset is described in this study. Geospatial and scientific requirements, methods used in dataset generation, and input data sources are described. Given the prominent role of precipitation in model calibration, accurate and consistent precipitation is a particularly high priority for the analysis. To evaluate the analysis from this perspective, its daily precipitation is compared with surface observing stations over 43 years. The analysis exhibits low bias compared with other similar products. It also displays nonstationary bias behavior after 2015 due to the lack of a climatological constraint, as well as frequent occurrences of heavy-to-extreme precipitation that are often difficult to verify. These findings should be taken into account when the product is used for model calibration.},
year = {2023}
}

\end{filecontents*}

\documentclass[9pt]{article}

\usepackage[margin=1in]{geometry}
\usepackage{authblk}
\usepackage{hyperref}
\usepackage{parskip}
\usepackage[numbers,sort&compress]{natbib}
\usepackage{xcolor}
\usepackage{longtable}
\usepackage{booktabs}

\newcommand{\fix}[1]{%
    \colorbox{red!20}{\textbf{Fix} --- #1}%
}
\newcommand{\note}[1]{%
    \colorbox{blue!20}{\textbf{Note} --- #1}%
}

\title{A spatially and temporally refined dataset of CONUS pluvial National Flood
Insurance Program claims}

\author[1]{Yuya Kawakami}
\author[2]{Tom Corringham}



\affil[1]{Department of Computer Science, University of California, Davis, Davis, CA}
\affil[2]{Scripps Institution of Oceanography, University of California, San Diego, La Jolla, CA}

\date{}

\begin{document}

\maketitle

\section*{Abstract}

The National Flood Insurance Program (NFIP) Redacted Claims dataset is
the primary public record of flood losses in the United States, but
two limitations of the raw data constrain its use for location- and
date-sensitive analysis. First, locations are deliberately obscured to
protect policyholder privacy. Each claim carries only a census block
group, a ZIP code, and a latitude/longitude rounded to one decimal
degree, rather than an exact address. Second, for some claims tagged as
rain-driven (pluvial) flooding, the reported loss date does not
coincide with any local rainfall event, likely reflecting reporting
delays or processing errors. Here we present a cleaning pipeline and
derived dataset that (1) converts all dollar amounts to a constant
reference year, (2) combines the three location signals into a single
bounded polygon per claim using a boundary-matching approach validated
against the data itself, and (3) corrects likely erroneous dates for
pluvial claims by checking each claim's local rainfall record against
NOAA's Analysis of Record for Calibration (AORC) precipitation data.
The pipeline is fully scripted and reproducible from public sources,
with all refinement choices exposed as configurable parameters.

\section*{Background \& Summary}

The NFIP, administered by the Federal Emergency Management Agency
(FEMA), is the principal source of insured flood-loss data in the
United States and is widely used in flood-risk, climate, and
disaster-economics research\fix{cite}.
%
FEMA publishes a redacted extract of individual claims (OpenFEMA's ``FIMA NFIP Redacted Claims v2'')~\cite{fema_nfip_claims_v2}, which is publicly available, freely accessible, and regularly updated.
%
However, redaction leaves several properties of the raw records unsuitable for location- or date-sensitive analysis without additional processing.

Location information is redacted for privacy. No street address is provided.
%
Instead, each record is accompanied by the claim's census block group FIPS code, its reported ZIP code, and a latitude/longitude pair rounded to one decimal degree.
%
These constitute three coarse approximations to the claim's true location, none of which alone constrains it tightly.
%
Past work using redacted NFIP claims has often relied on this rounded latitude and longitude alone as a proxy for claim location.
%
However, analyses that would benefit from more precise location information\fix{cite} require higher spatial resolution than this proxy affords, particularly for the understudied case of pluvial flooding.

Reported loss dates are also imperfect for a subset of claims\fix{cite}.
%
NFIP identifies pluvial (rain-driven) flooding claims via \texttt{causeOfDamage == "4"}.
%
However, for these claims, the reported \texttt{dateOfLoss} does not always coincide
with a day of actual rainfall at the claim's location\fix{cite}.
%
This pattern is more consistent with reporting delays, adjuster lag, or transcription error
than with the true date of the flood.

To advance the modeling of pluvial flood incidence, risk, and associated damages, this paper describes an open, reproducible pipeline that addresses both problems using only public data, namely FEMA's own claims extract, the Federal
Reserve Economic Data (FRED) personal consumption expenditures price
index, U.S. Census Bureau cartographic boundary files, and NOAA's AORC
v1.1 precipitation reanalysis~\cite{fall_analysis_2023}. 
%
The resultant dataset is thus a spatially and temporally refined dataset of pluvial NFIP claims from 1979-02-01 onwards, where we associate each reported pluvial claim with a polygon that bounds the claim's true location and, where applicable, a corrected date of loss.
%
We further design the dataset construction script so that it exposes pertinent refinement parameters that can be tuned to different assumptions and need of the downstream tasks.
%
Both the dataset and the pipeline code that produced it are released under an open license.

\section*{Methods}

The pipeline processes FEMA's public claims extract in three ordered
stages, namely inflation adjustment, spatial triangulation, and pluvial
date correction. Each stage is implemented as an independent,
re-runnable script. The full sequence is defined in
\texttt{src/run\_pipeline.sh} in the code repository (see Code
Availability).

\subsection*{Input data}

\begin{itemize}
  \item \textbf{FEMA OpenFEMA} --- FIMA NFIP Redacted Claims v2~\cite{fema_nfip_claims_v2}. This is provided in the public domain by the Federal Emergency Management Agency (FEMA) with no authentication required. 
  \item \textbf{FRED} (Federal Reserve Bank of St. Louis) --- Personal
    consumption expenditures price index, series
    \texttt{DPCERD3Q086SBEA}~\cite{fred_dpcerd3q086sbea}. This is provided in the public domiain a s a CSV file with no authentication required.
  \item \textbf{U.S. Census Bureau} --- Cartographic boundary shapefiles, block groups and ZIP Code Tabulation Areas (ZCTAs). This is made available per decadal census. Its matching strategy with the NFIP claims is configurable as flags withe the default behavior below. This is provided in the public domain with no authentication required. 
  \item \textbf{NOAA} --- Analysis of Record for Calibration (AORC) v1.1~\cite{fall_analysis_2023}, 30 arc-second ($\sim$800 m) grid, hourly. This is made available via Amazon Web Services (AWS) at\texttt{s3://noaa-nws-aorc-v1-1-1km}. No authentication is required.
\end{itemize}

% None of these sources is redistributed in raw form in the code
% repository. Each pipeline step downloads, or expects a locally staged
% copy of, its input, documented at the point of use.

\textbf{Scope.} This dataset covers the 48 contiguous U.S. states and
the District of Columbia (CONUS). NFIP claims also exist for Alaska,
Hawaii, Puerto Rico, and other U.S. territories. These are excluded by
design, not as an incidental side effect of processing, because both
the Census cartographic boundary conventions and AORC's grid extent
treat CONUS as a distinct product from these territories. The released
file is further restricted to claims coded \texttt{causeOfDamage ==
"4"}, matching the pluvial focus of the date-correction stage. This
restriction is a pipeline-invocation choice rather than a limitation of
the triangulation method itself. Omitting the cause-of-damage filter at
run time triangulates every claim regardless of cause.

Table~\ref{tab:claim-count-funnel} records the claim count remaining
after each of these restrictions, from FEMA's inflation-adjusted
extract through to the triangulation output. Entering triangulation
also requires both \texttt{censusBlockGroupFips} and
\texttt{reportedZipCode} to be non-null, regardless of whether a
latitude/longitude pair is present, a stricter condition than requiring
only one of the three location signals to be available.

\begin{table}[h]
\centering
\begin{tabular}{lrrr}
\toprule
Step & $n$ claims & $\Delta$ from previous & \% dropped \\
\midrule
Original dataset, all causes & 2{,}721{,}780 & --- & --- \\
\texttt{causeOfDamage == "4"} (pluvial) & 1{,}206{,}336 & $-$1{,}515{,}444 & 55.68\% \\
Both \texttt{censusBlockGroupFips} and \texttt{reportedZipCode} present & 1{,}163{,}030 & $-$43{,}306 & 3.59\% \\
CONUS filter (drops AK, HI, PR, other territories) & 1{,}155{,}473 & $-$7{,}557 & 0.65\% \\
Triangulation output (drops rows w/ empty intersection) & 1{,}149{,}288 & $-$6{,}185 & 0.54\% \\
\bottomrule
\end{tabular}
\caption{Claim count remaining after each filtering and processing
step, from FEMA's inflation-adjusted extract through triangulation, for
the pluvial CONUS population this dataset covers.}
\label{tab:claim-count-funnel}
\end{table}

\subsection*{Stage 1 --- Inflation adjustment}

Every dollar-denominated claim field (paid amounts, coverage limits,
building/contents value, replacement cost) is rescaled to a fixed target
year's dollars using the FRED PCE price index, matched to each claim's
loss quarter. The default target year is 2021 and is configurable.

\subsection*{Stage 2 --- Spatial triangulation \emph{(Spatial refinement)}}

Each claim carries three independent, imprecise location signals,
namely a census block group FIPS code, a ZIP code, and a rounded
latitude/longitude. Triangulation intersects whichever of these signals
are available into a single polygon per claim, yielding a strict upper
bound on the claim's true location that is typically substantially
smaller than the region implied by any single source alone.

\textbf{Boundary assignment.} Here and throughout the rest of this
paper, a \emph{release} means a specific dated edition of a Census boundary
shapefile, for example the 2010 or 2020 block-group shapefile, and is
unrelated to the release of this dataset itself. Census block-group
and ZCTA boundaries are redrawn each decennial cycle, so multiple
releases exist for a given geography.
%
Rather than matching each claim to the boundary release nominally in effect at its loss year,
boundary-release assignment is a run-time choice, selected
independently for block group and ZCTA via a strategy parameter with
four options.

\begin{itemize}
  \item \texttt{default} --- for block group, the 2010 release before a
    configurable cutover year and the 2020 release from that year on.
    For ZIP, dropped entirely before 2000 and otherwise the most recent
    configured ZCTA release from 2000 on.
  \item \texttt{closest} --- whichever configured release's year is
    numerically nearest the claim's loss year.
  \item \texttt{most\_recent} --- always the newest configured release,
    regardless of loss year.
  \item \texttt{drop} ---  do not use that source.
\end{itemize}

The released dataset uses \texttt{default} for both
block group and ZCTA. 
%
Offical block group shapefiles do not exist as a nationwide census before 1990, and ZCTA shapfiles exist only from the 2000 census onward\fix{cite}.
%
Under \texttt{default}, pre-2000 claims skip the ZIP source
entirely and fall back to block group and latitude/longitude polygon
only, while the block-group source is still attempted against the 2010
release regardless of how far the loss year precedes 1990.

Once a \emph{release} is selected, or unselected in the case of \texttt{drop}, we compute the spatial intersection of the boundaries.
%
In the worst case, we fall back to the $0.1 \times 0.1$ degree box implied by the rounded latitude/longitude, but in almost all cases the intersection of the three sources is a substantially smaller polygon.
%
Our achieved spatial refinement is, however, not uniform in space and significantly favors urban regions with dense block-group and ZIP coverage over rural regions with sparse coverage.

\subsection*{Stage 3 --- Pluvial date correction \emph{(Temporal refinement)}}

For each claim coded \texttt{causeOfDamage == "4"}, the reported
\texttt{dateOfLoss} is checked against whether it coincides with a day
of meaningful precipitation (maximum hourly precipitation exceeding a
threshold, default 5.0 mm) at the claim's triangulated location, using
AORC v1.1. If it does not, a window of nearby days (default $\pm$7) is
searched for the wettest day, and if that day clears the threshold it
is treated as the corrected date. The original \texttt{dateOfLoss} is
never overwritten. The corrected value and an explicit status code are
added as new columns, letting downstream users decide whether to apply
the correction.

The precipitation threshold used to judge whether a day counts as
meaningful rainfall is configurable, either as a single constant or as
a spatial field.
%
This should reflect the prior belief, assumptions and needs of the downstream tasks.
%
A spatial field may be useful as there is no principled reason a claim in an arid region and a claim in a persistently wet region should require the same amount of rainfall to qualify as \emph{meaningful}.

\begin{itemize}
  \item \textbf{Constant} --- the default, a single value applied
    everywhere. The script defauls to 5.0 mm but this is configurable at run time.
  \item \textbf{Spatial field} --- a user-supplied NetCDF raster
    sampled at each claim's exact location, giving a spatially varying
    threshold without requiring the raster to share AORC's grid or
    resolution.
\end{itemize}

Day boundaries are evaluated in each claim's own local time zone,
resolved geographically per claim location, rather than a single
national reference clock, and daylight-saving transitions are handled.
% 
Precipitation at each claim's triangulated polygon's centriod location is computed by bilinear interpolation of the four surrounding AORC grid pixels.

AORC coverage for CONUS begins 1979-02-01. Pluvial claims before this
date, and claims located outside the AORC CONUS grid extent, are not
evaluated and thus excluded from the resultant dataset.

\section*{Data Records}

The dataset is released as a single columnar file
(\texttt{claims\_pluvial\_corrected.parquet}, Apache Parquet format) at
claim-level granularity. All original columns from the FEMA OpenFEMA
\texttt{FimaNfipClaims} extract are preserved unchanged (see FEMA's
data dictionary for definitions). The columns added by this pipeline
are as follows.

\textbf{Added by inflation adjustment.}
\begin{itemize}
  \item \texttt{\{field\}Real2021} for each of 15 dollar-denominated
    fields (paid amounts, coverage limits, building/contents value,
    replacement cost), rescaled to 2021 dollars.
  \item \texttt{amountPaid} / \texttt{amountPaidReal2021} --- sum of the
    three \texttt{amountPaidOn*Claim} fields.
  \item \texttt{damageAmount} / \texttt{damageAmountReal2021} ---
    \texttt{buildingDamageAmount + contentsDamageAmount}.
\end{itemize}

\textbf{Added by triangulation.}
\begin{itemize}
  \item \texttt{geometry} (polygon, EPSG:5070 Albers Equal Area) --- the
    intersection of whichever of \{block-group boundary, ZCTA boundary,
    lat/lon box\} were available and spatially validated for the claim.
    Claims with zero contributing sources, or whose sources fail to
    overlap one another (see \texttt{geometry\_is\_empty} below), are
    excluded from the released file.
  \item \texttt{n\_geometry\_sources} (0--3) and
    \texttt{geometry\_sources} (e.g. \texttt{"block\_group+zip+latlon"})
    --- record which sources contributed.
  \item \texttt{geometry\_is\_empty} --- true when a claim's sources
    each individually validated against the reported lat/lon but do not
    overlap one another. Such claims are dropped before the released
    file is written, and the column is retained for downstream QA.
  \item \texttt{block\_group\_vintage\_used} and
    \texttt{zip\_vintage\_used} --- the boundary-release year (e.g.\
    1990, 2000, 2010, or 2020) selected for the claim by the
    block-group and ZIP release-matching strategies, respectively (see
    Methods). The two need not agree, even when both strategies are
    left at \texttt{default}.
  \item \texttt{block\_group\_match\_status} --- one of
    \texttt{not\_found}, \texttt{validated},
    \texttt{unvalidated\_no\_latlon}, \texttt{spatially\_inconsistent},
    \texttt{dropped\_by\_strategy}, or \texttt{no\_vintage\_selected}.
  \item \texttt{zip\_match\_status} --- the same status values as
    \texttt{block\_group\_match\_status}, plus
    \texttt{before\_zcta\_coverage} for claims that predate ZCTA
    coverage under the \texttt{default} strategy.
\end{itemize}

\textbf{Added by pluvial date correction.}
\begin{itemize}
  \item \texttt{correctedDateOfLoss} --- the reported date if it
    already coincided with meaningful precipitation, the corrected date
    if a correction was made, or the original date otherwise (always
    populated).
  \item \texttt{pluvialCorrectionStatus} --- one of
    \texttt{not\_pluvial}, \texttt{before\_aorc\_coverage},
    \texttt{outside\_aorc\_grid\_extent}, \texttt{accepted\_as\_reported},
    \texttt{corrected}, \texttt{no\_qualifying\_precip\_in\_window}, or
    \texttt{no\_aorc\_data\_in\_window}.
  \item \texttt{pluvialCorrectionMaxPrecipMm} --- the maximum daily
    precipitation (mm) found at the claim's location on the date
    \texttt{correctedDateOfLoss} refers to.
\end{itemize}

\texttt{geometry} uses EPSG:5070 (meters). The original FEMA
\texttt{latitude} / \texttt{longitude} fields remain in WGS84
(EPSG:4326) degrees, unchanged.

\section*{Data Overview}

TBD

\section*{Technical Validation}

\subsection*{Boundary-release assignment}

Throughout this subsection, each claim's reported latitude and
longitude, rounded by FEMA to one decimal degree, is treated as ground
truth for the claim's true location, giving a $0.1 \times 0.1$ degree
box the claim must fall within. For a given release, Match is the raw
rate at which a claim's block-group or ZIP code string matches a GEOID
in that release's shapefile. Valid is computed only over the matched
claims, and reports the share of those matches whose polygon also
overlaps the claim's ground-truth lat/lon box, exactly the
spatial-consistency check described in Methods. A high Match rate with
a low Valid rate would mean the GEOID exists in that release but points
to the wrong place. A high Valid rate confirms that when a release
matches at all, it matches to a polygon consistent with the claim's
own reported coordinates.

\textbf{Block-group boundaries.} Restricted to the released
(pluvial, \texttt{causeOfDamage == "4"}) claim population, matching
\texttt{censusBlockGroupFips} against the official 2010 decennial
block-group release yields a 93.3--100\% match rate for every loss
year from 1978 through 2015, remaining the better-matching release
through 2019 even as its raw match rate declines to 84.3--87.5\% over
2016--2019. This indicates FEMA's geocoding assigns a recent-release
boundary to each record at processing time rather than using the
historical loss-year boundary. In absolute terms the transition to the
newer (2020) release is not sharp. The raw match rate against 2020
climbs gradually with loss year rather than jumping at a single point,
reaching only 83.2\% in 2020 itself before climbing to 100\% by 2026.
Ranked against each other, however, the 2010 release is the strictly
better-matching release for every loss year from 1978 through 2019,
and the 2020 release becomes the strictly better-matching release for
every loss year from 2020 through 2026, with no exceptions across this
population (Table~\ref{tab:release-match-rate-bg}). This clean
crossover is evidence supporting a strong working hypothesis, namely
that FEMA's geocoding service itself assigns the 2010 release to
claims with loss year before 2020 and the 2020 release to claims with
loss year 2020 or later, even though general match-quality variation
keeps the raw match-rate curve gradual rather than a step. This is
exactly the two-release cutover \texttt{default} implements for block
group, which is why \texttt{default} uses a single
cutover year (2020) rather than a historical loss-year rule.

\textbf{ZCTA boundaries.} ZCTAs exist officially only from the 2000
census onward\fix{cite}. Of this population's claims, 1,148,472 carry
a well-formed five-digit \texttt{reportedZipCode} and are eligible to
be tested against ZCTA boundaries at all, the remainder having an
empty or malformed ZIP field, a data-completeness gap in the source
records concentrated in the 1970s and 1980s rather than a
boundary-matching failure. Within this eligible set, match rates are
high and far more stable across releases than for block group, ranging
95.6--99.5\% for the 2000 release and
98.1--100\% for the 2010 and 2020 releases across every loss year
(Table~\ref{tab:release-match-rate-zcta}). \texttt{default}, which
always uses the most recent configured release from 2000 onward,
matches 99.61\% of these claims. Testing all three available ZCTA
releases per claim and keeping the best match recovers only 8
additional matches (0.001\%) over \texttt{default} alone, confirming
that release choice has little effect on ZCTA match rate, unlike the
block-group case above. The \texttt{zcta\_strategy} parameter remains
configurable, and users who want to test \texttt{closest} or
\texttt{most\_recent} against \texttt{default} directly can do so.

\input{tables/boundary_release_block_group.tex}

\input{tables/boundary_release_zcta.tex}

\textbf{Extent of location refinement.} \note{Quantify how much
triangulation reduces polygon area relative to each individual source,
whether this reduction is spatially uniform across CONUS, and whether
its magnitude affects downstream use.}

\textbf{Pluvial precipitation threshold.} A synthetic two-region
threshold field (5 mm west of $-95^{\circ}$ longitude, an unreachable
999 mm east of it) confirms the spatial threshold lookup is applied per
claim rather than falling back to a single value. The correction rate
fell from 16.8\% under the uniform 5 mm threshold to 4.3\% under the
synthetic spatial threshold. \note{A full sensitivity sweep of the
precipitation threshold and search-window size across realistic values
is still planned.}


\section*{Data Availability}

The derived dataset described in this paper is released under a CC-BY
4.0 license. The three upstream sources it is
built from are also public, namely FEMA's OpenFEMA FIMA NFIP Redacted Claims v2
(\url{https://www.fema.gov/openfema-data-page/fima-nfip-redacted-claims-v2}),
U.S. Census Bureau cartographic boundary files
(\url{https://www.census.gov/geographies/mapping-files/time-series/geo/carto-boundary-file.html}),
and NOAA's AORC v1.1 (public S3, \texttt{s3://noaa-nws-aorc-v1-1-1km},
no-sign-request). None impose redistribution restrictions beyond those
already stated.

\section*{Code Availability}
\note{Repo location TBD}

\bibliographystyle{plainnat}
\bibliography{manuscript}

\section*{Author Contributions}

\section*{Competing Interests}

\section*{Acknowledgements}
\section*{Funding}


\end{document}
